{
  "name": "carbifyio-frontend",
  "version": "1.0.0",
  "description": "Carbifyio frontend — Vanilla JS SPA with Vitest unit tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src/js/**/*.js --max-warnings=0"
  },
  "devDependencies": {
    "@eslint/js": "^9.5.0",
    "@vitest/coverage-v8": "^4.1.8",
    "@vitest/ui": "^4.1.8",
    "eslint": "^9.5.0",
    "jsdom": "^24.0.0",
    "vitest": "^4.1.8"
  }
}
